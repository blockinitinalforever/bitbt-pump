# UI 原版基准（不作为生产页面发布）

来源：用户提供的 BitBT-Launchpad-UI-Complete-2026-09-11.zip。

这里的 HTML 是交付原件，保留原版 28 个页面、三个样式块和交互原型。
其中金额、持仓、收益、价格和创建结果是演示数据；不要直接部署或接入真实钱包执行交易。

实施方向：以此文件为页面基底，真实功能适配到原版容器。
数据内容、权限状态与真实风控参数可以不同，布局、字体、间距、图标、导航顺序和响应式规则以原件为准。

运行 `node scripts/audit-ui-reference.mjs` 查看生产源文件与此原件的逐页结构、样式哈希差异。
`--check` 在有差异时返回失败；当前尚未完成整页迁移，不要把原版自身检查通过理解为真实数据版完成。

最终验收包括桌面与手机渲染比对，以及钱包、上传、发币、买卖、永续和个人记录等真实功能。
静态结构检查不能替代浏览器渲染和功能验收。

原包已知缺件：HTML 引用了 assets/icons/lucide/plug-zap.svg，但原 ZIP 的 assets 目录也未包含该文件。应单独补齐原图标，不改成另一种视觉设计。
