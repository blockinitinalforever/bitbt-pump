# 2026-09-14 最新 UI 核对结果

## 当前唯一视觉基准

用户目录：`/Users/dylanpoe/Downloads/Telegram Desktop/BitBT-Launchpad-UI-Complete-2026-09-14`。

已完整读取 AGENTS、README、PAGE-STRUCTURE 和机器可读页面清单；将原版 HTML 与交付说明保存到 `design-reference/20260914/`，不对外提供原型页面，不覆盖用户原件。

## 2026-09-14 最终统一复核

- 28 个原稿页面全部保留，三个原稿 CSS 区块逐字节一致；额外保留 `perpetual` 高级操作和 `vault-store` 两个真实功能页。
- 代币详情恢复原稿四项首屏统计；其余真实指标与项目资料移入原稿“数据”区域，不删除 API 数据。
- 发布确认恢复“代币 / 发行网络与曲线 / 费用”三个主要区块；Factory、Salt、预测地址和收款地址等完整签名参数收纳到展开区。修复计价资产曾错误显示为代币符号的问题。
- 公告详情恢复生效时间、适用网络和创建入口；API 未提供网络时明确显示“公告未指定”，不填原稿示例值。
- 时区、价格精度与减少动态效果均已接入本地显示偏好。更改显示设置只重绘缓存数据，不请求 RPC、不刷新报价、不修改交易输入和签名快照。缺少可信实时汇率时 CNY/USDT 换算保持禁用，避免虚假估值。
- 动态中英文文案已在程序拥有的渲染点处理；用户代币名称、地址、金额及未知后端诊断保持原文。图表时间轴随时区更新，显示精度只影响呈现，不反写交易数值。
- 30 页按 1440px/390px 分组复核，8 个关键页面另按 1100/760/560/440px 复核；没有横向溢出、错误弹窗遗留或主要按钮不可达。正式 `/pump` 外层入口在 1440/1100/800/760/390px 通过。
- 全量 164/164 测试、TypeScript、CSS 校验和 `git diff --check` 通过；生产构建完成。四个自有前端脚本均已进入控制流混淆产物，source map 关闭。

本轮未部署、未发送链上交易。真实 API 数据、钱包内容和用户要求的四个底部联系方式会使文字与原稿示例不同；这些属于明确保留的业务内容，不是视觉体系偏离。

## 与上次参考及当前实现的对比

- 新版与上次参考的三个 CSS 区块逐字节一致；28 个页面的元素层级及样式 class 一致。此结论只涉及 CSS / 布局，不代表脚本、全部属性或文案完全相同。
- 当前实现的三个原版 CSS 区块也与新版一致；另保留真实钱包、错误反馈等功能所需扩展样式，仍需检查扩展是否影响最终视觉。
- 新版 assets 共 391 个文件，当前资源目录全部存在；除两个资源清单文件内容有差异外，其余 389 个文件字节一致。资源清单差异不能视为图片差异。
- 基础 CSS、主要框架和导航按原稿恢复；静态全 DOM 严格相等仍不是通过状态：真实列表条数、额外业务字段、错误与钱包控件会产生合理差异。不能把“基础样式一致”或“自动几何通过”描述为所有像素完全相同。
- 收入、开发者、邀请、提醒已采用新版内部页面 ID；旧链接通过别名映射兼容。邀请页原版布局已迁入，保留实际表单和数据源。
- 已补齐 success / action-center，原稿 28 个页面 ID 全部存在。success 仅显示本会话真实确认的发布结果，无回执时明确为空；已有发币后 canonical 代币地址跳转保留。action-center 保留原版框架与真实功能导航，交易确认仍使用安全快照和钱包流程。
- 原有 perpetual 高级操作、Vault Store 等额外真实功能必须保留，不为凑原型页面数量而删除。

## 本轮继续对齐

1. 税费页面恢复原版四个税率快捷选项、三张方案卡；独立买卖税、分配及反机器人设置收纳在可展开的高级设置，不删字段。
2. 恢复 create-mode 风险说明；发布确认中长地址、Salt 和描述允许换行，不挤出页面。
3. 按原稿 mainScreens 恢复底部导航显隐：仅 discover/live/rank/create-mode/profile 显示五 TAB；交易、发币步骤等子页收起。此前所有子页始终显示 TAB 的实现已纠正。
4. 永续保证金单位、杠杆上限和刻度绑定市场；25%/50%/75%/MAX 读取钱包余额填表，不发送交易。保留独立创建市场与池子的实际条件校验。
5. 修复“我的发射、交易记录、排行榜”空态提示重复插入，避免刷新导致页面越变越长。
6. 从原稿提取静态中英文词汇，切换时不替换控件、不覆盖钱包或用户数据。动态 API 文案仍由业务渲染负责，不宣称所有后端错误已本地化。
7. 新增 ui-locale.js 自动进入递归混淆清单，已实际生成控制流混淆产物。

## 验证与证据

- 最新全量测试 153/153 通过；TypeScript、git diff --check 通过。
- 混淆清单包含 launch-logo-upload.js、launchpad-live.js、ui-locale.js、walletconnect-provider.js；本轮真实生成完成。
- 中文 30 页 × 1440/390/350px、900px 高；英文相同 30 页与宽度、600px 高，共 180 场景通过。
- 检查页面路由、钱包恢复、溢出、原版五 TAB 显隐、触控尺寸、底部联系方式，以及滚动后的主要提交按钮可达性。
- 中文截图：`/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-layout-XVegxi`。
- 英文短视口截图：`/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-layout-TOKDhH`。
- 使用独立模拟钱包和只读 fixture，未发真实交易；不能代替 OKX/TP 手机实机签名验收。

## 保留的业务差异

- 比原稿更多的合法报价资产、分类、社区链接、税费参数和完整交易快照保留。
- 原稿模拟收益、金额、持仓、未支持的委托方式不作为真实能力展示。
- 保留用户要求的四个全站底部联系方式与两个高级页面。
- 完整的全像素人工对照及真实手机钱包验收没有被上述自动检查替代；不得宣称无条件完全 1:1。

当前未部署，未执行链上交易。

## 再检查：正式入口与断点

- 原始交付文件 SHA 与当前 design-reference 一致；本地 HTML 相对 src/href 无缺失。三个原版 CSS 区块未修改。
- 增加 1100/760/560/440px 关键页面断点与网络菜单命中检查；BNB/Robinhood 选项均可见无遮挡。
- 发现内层页面检查未覆盖的外层问题：`/pump` iframe 最低高度 720px 在较矮桌面窗口超出外层；已改为按视口计算且允许收缩，保留原有 iframe 安全权限。
- 正式入口复核又发现手机页头实际高度与写死的减法不一致，底部部分按钮被裁切。改为移动端纵向 flex 分配剩余高度，页头/导航/底栏不压缩，内容区可收缩，不改原稿字体、配色和卡片样式。
- 正式入口 1440/1100/800/760/390px、600px 高度共 5 个场景通过。桌面保持原稿整页滚动，联系方式滚动可达；手机底栏完整留在视口内。截图：`/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-shell-CLd6oi`。网络被屏蔽，截图中的网络不可用提示属于检查环境，不是线上服务结论。
- 新增 `scripts/check-ui-shell.mjs`，避免只检查内层页面遗漏外层裁切。未部署，未发送链上交易。
- 两项修复后：全量 161/161 测试、32 个临界宽度场景、5 个正式入口场景及英文超限弹窗检查均通过；控制流混淆产物已重新生成。

## 网络说明、联系方式与错误文案

- 已补齐发行网络说明、报价资产说明、迁移规则及底部联系方式中英文；官网/邮箱 href 和 Telegram、X 目标保持不变，网络名称与英文前后片段保留空格。
- DEX 选项按语言更新标签，保留当前选中值和发布快照；语言切换不重新加载接口。
- 所有 friendlyError 固定分支已提供英文映射：登录过期、钱包拒绝/待处理、Factory 不匹配、市场限额、reduce-only、文件过大、限流、超时、断网和服务错误等。
- 动态单仓限额和余额不足提示保留原始计算式、金额、资产及差额；未知后端诊断以“Reported details (original)”保留原文，避免猜测原因或丢失技术详情。
- 打开的操作弹窗随语言重新渲染标题、状态和正文；没有替换用户输入或交易参数。
- 全量 160/160 测试通过，最终文案专项测试通过，类型检查通过；英文 90 页场景通过。
- 350px × 600px 实际本地浏览器点击超限开仓，英文弹窗展示 `100 × 10 = 1000` 和 `current limit: 100`，Got it 可达，未发送交易或写入 API。截图：`/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-layout-fyWSqL/error-copy-350.png`。
- 此项完成不代表其他高级业务页面所有动态文案已覆盖；未部署。

## 动态文案与基础表单继续对齐

- 永续市场搜索提示、结算说明、Keeper 待命/准备提示、市场数量、实时数据摘要、发行网络选择标签按当前语言渲染；语言切换只用现有状态重绘，不触发新的 API/RPC 请求。
- 翻译仅在应用拥有的文案生成点执行，未对用户名称、地址、金额或任意后端错误做全 DOM 替换。
- 原稿基础表单顺序恢复为名称、符号、故事、社交链接；额外项目分类与 Discord 收入可展开的“更多项目资料”，字段 ID、取值和原有校验监听保留。
- 补充可选字段、长度提示和高级设置静态英文词条。其余未覆盖动态错误和高级业务文案仍待继续补齐，不宣称全站双语已完成。
- 全量 158/158 测试通过。浏览器往返中英文确认输入原样保留，新增请求数为 0；过期检测改为有上限地等待实际会话清空，仍检查全部提交按钮禁用，不用固定 700ms 代替结果。
- 本轮最终英文 30 页 × 1440/390/350px 短视口检查通过（`/tmp/bitbt-ui-locale-final-en.json`），包括语言往返、会话过期及按钮禁用；类型检查和控制流混淆产物生成通过。人工截图仍发现网络说明及底部联系方式部分中文未翻译，继续列为未完成项。

## 公告页继续对齐

- 对比发现实际差异：旧实现将公告详情插在列表末尾，缺少原稿的列表标题；这不是数据替换所必需的差异。
- 已恢复详情在前、列表在后的原稿顺序，增加真实总数和未读数；默认选择当前分类置顶公告或首条，点击列表切换详情并记录已读。
- 无公告时详情隐藏且清空，不保留旧分类内容；没有复用原稿虚构创建费、时间或适用网络。
- 最新测试 156/156，类型检查和差异空白检查通过；中文短视口 30 页 × 3 宽度共 90 场景通过。截图：`/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-layout-f8vTiv`。
- 同配置英文 90 场景通过，中英文合计 180；本轮控制流混淆产物已重新生成成功。
- 仍不宣称完全 1:1：英文动态文案尚有混用；额外真实表单字段、全局联系方式和高级页面需与原稿区别标注。没有执行部署。

## 继续核对：显隐冲突与发币草稿预览

- 人工截图发现原版 flex/grid 覆盖浏览器 hidden 默认样式，BSC 发行参数页显示了不应显示的报价资产；扩展样式增加作用域内 hidden 强制隐藏，未修改三个原版 CSS 区块。
- 浏览器逐页检查新增 exposedHidden 指标；中文 90 场景通过，没有带 hidden 属性却仍占位的元素。
- 用户填写的名称/符号保留在发行步骤草稿预览；参数变更依然清空签名快照并禁用发布，不把草稿误当成已校验结果。
- 最新全量测试 155/155 通过。未处理测试币转账，未部署。
- 英文短视口 90 场景亦通过 hidden 可见性检查；混淆产物生成通过。
- 发行参数预览恢复原稿网络标签，绑定当前选中网络；长名称网格单元允许换行，不改变原版字号或列宽。24 位名称和 10 位符号在 1440/390/350px、600px 高度浏览器检查通过，无预览区域溢出，钱包跳转/过期校验仍通过。截图在 `/var/folders/7k/ygq848zd61l1jbt8hyyl0py00000gn/T/bitbt-ui-layout-v6qqaq`。
